// calculate the gcd or hcf of the two integers
#include <stdio.h>


int gcd(int, int);
int main (void) {

	int x, y;
	printf("Enter two numbers: \n");
	scanf("%d, %d", &x, &y);

	ans = gcd(x, y);
}

int gcd(int x, int y) {

	if (x< y) {
		
	}
}